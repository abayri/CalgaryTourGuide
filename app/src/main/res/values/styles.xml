<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/primary_dark_color</item>
        <item name="actionBarStyle">@style/GuideAppBarStyle</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Style to show no elevation from app bar -->
    <style name="GuideAppBarStyle" parent="style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="elevation">0dp</item>
    </style>

    <!-- Style for text in a list item -->
    <style name="TextStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textAppearance">?android:textAppearanceSmall</item>
    </style>

    <!-- Style for Category tab -->
    <style name="CategoryTab" parent="Widget.Design.TabLayout">
        <item name="android:background">@color/primary_color</item>
        <item name="tabIndicatorColor">#FFFFFF</item>
        <item name="tabSelectedTextColor">#FFFFFF</item>
        <item name="tabTextAppearance">@style/CategoryTextAppearance</item>
    </style>

    <style name="CategoryTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">#212121</item>
    </style>

</resources>
